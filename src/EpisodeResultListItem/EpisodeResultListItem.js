import React from "react";
import PropTypes from "prop-types";

const EpisodeResultListItem = (props) => {
  return (
    <div>
      <p>EpisodeResultDetail</p>
    </div>
  );
};

EpisodeResultListItem.propTypes = {};

export default EpisodeResultListItem;
