import React from "react";
import PropTypes from "prop-types";

const CharacterResultList = (props) => {
  return <div>CharacterResultList</div>;
};

CharacterResultList.propTypes = {
  data: PropTypes.arrayOf(PropTypes.shape),
};

export default CharacterResultList;
